import { SyncDecision } from "@/components/sync/SyncDecision";

export default function DecisionsPage() {
  return (
    <div className="flex-1 flex flex-col h-full overflow-hidden">
      <SyncDecision />
    </div>
  );
}
